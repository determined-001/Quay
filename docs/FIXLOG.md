# Fix Log

Every merged bug fix must have a row here. The rule: **no bug fix merges without
a regression test and a row in this table.**

Columns:
- **Date** — merge date (YYYY-MM-DD)
- **Symptom** — what the user/operator observed going wrong
- **Root cause** — why it went wrong
- **Fix commit** — the commit that resolved it
- **Regression test** — the test (file · name) that now prevents the defect from returning

---

| Date | ID | Symptom | Root cause | Fix commit | Regression test |
|------|----|---------|------------|------------|-----------------|
| 2026-07-14 | BUG-3.1 | Off-ramp jobs in `offramp_pending` were silently abandoned on API restart; sellers were stuck waiting for a cash-out that would never advance | `startCashOutPoller` was only wired up inside `container.start()`, which was called correctly, but a process restart left any in-flight `offramp_pending` links frozen until the next poll cycle — which never came because the poller interval was not restarted with the right state on boot. The root cause: the poller depended on in-memory `setInterval` state that was discarded on exit with no re-queuing of persisted pending jobs at startup | `e78b89a` | `packages/offramp/test/testanchor.test.ts` · *"initiate() then status() completes the SEP-6 request/response round trip"* (live); `packages/core/test/status.test.ts` · *"allows off-ramp retry after failure"* (guards the `offramp_failed → offramp_pending` re-entry that makes retry possible) |
| 2026-07-14 | BUG-1.4 | The deployed web app showed "Create link" but every attempt silently failed for users other than the developer; no error was visible, the network call just hung or errored against `http://localhost:8787` | `NEXT_PUBLIC_API_URL` was not set when the Vercel build ran, so `apps/web/lib/api.ts` baked the `http://localhost:8787` fallback into the client bundle at build time. Any browser that was not the developer's machine could not reach the API at all. Noted explicitly in `MAINTAINER.md` as the "Nester-W6 demotion pattern" | `9cfc492` | `apps/web/lib/api.ts` — `apiBase()` is covered by the build-time env check; see deploy checklist in `MAINTAINER.md` section 0 (`NEXT_PUBLIC_API_URL` must be set before the Vercel build step) |
| 2026-07-14 | BUG-2.5 | A payment that arrived on-chain was not matched to its link even though the correct memo was present; the watcher logged `no_memo` | When Horizon's `transaction()` call threw (e.g. transient 500, rate-limit), the `catch` block in `packages/stellar/src/normalize.ts` left both `memo` and `memoType` as `null`. The matcher in `packages/core/src/matching/match-payment.ts` treats `memo === null` as `no_memo` and parks the payment. The next poll advanced the cursor past the transaction, so it was never retried — the link stayed `active` indefinitely | `e78b89a` | `packages/core/test/match-payment.test.ts` · *"returns no_memo when memo missing"* (verifies the `null` / `"none"` path); the fix ensures the transaction is re-fetched on the next tick by **not** advancing the cursor when a network error occurs during normalization |
| 2026-07-18 | BUG-MAINT-8 | Live demo URL (`stellar-checkout-api.fly.dev`) did not resolve; anyone following the README hit a dead API and a broken "Create link" flow | API was never deployed to the target host referenced in the earlier web bundle. The Fly.io target was abandoned without deploying; the web build still pointed at it via the stale `NEXT_PUBLIC_API_URL` value (or the `localhost` fallback). Described in `MAINTAINER.md` line 8 as the critical pre-entry blocker | `9cfc492` | Deploy smoke-test (manual): `GET /health` on the new Render URL must return `{"ok":true}` before any wave-entry snapshot; checklist item added to `MAINTAINER.md` section 0 |
| 2026-08-06 | BUG-4.11 | The deployed API was down continuously from ~2026-07-31 — 39 consecutive uptime-check failures, create-link synthetic at 14.49% over 90 days, `docs/STATUS.md` red. The web dashboard stayed up, so a visitor reached a working UI whose every action failed | `render.yaml` sets `OFFRAMP: testanchor`, which makes `apps/api/src/env.ts:159` resolve `KYC_ENCRYPTION_KEY` with `req()` at module load — but the blueprint never declared that variable, so the container threw `Missing required env var: KYC_ENCRYPTION_KEY` before listening and crash-looped. Three further secrets (`WEBHOOK_SECRET_ENCRYPTION_KEY`, `JWT_SECRET`, `SERVER_SIGNING_SECRET`) were also undeclared, and `HOME_DOMAIN` was unset so SEP-10 challenges and `stellar.toml` advertised `localhost:8787` | `c4e84d5` | `apps/api/test/secret-crypto.test.ts` · *"throws in production when the key is missing"* and *"throws in production when the key is present but malformed"* — `createContainer()` now calls `assertKeyConfigured()` at boot, so a missing key fails the deploy instead of the first customer webhook. Blueprint completeness is covered by the manual smoke-test below |
| 2026-08-06 | BUG-8.9 | `pnpm test` failed on a maintainer's machine with `Missing required env var: KYC_ENCRYPTION_KEY` across 4 test files (`demo-route`, and all three `watcher-loop` suites) while CI stayed green on the same commit — so the suite could not be trusted locally and a real regression could hide behind the divergence | `apps/api/src/env.ts` calls `loadEnvFiles()` at module import, walking up to the nearest `.env`. Any test that transitively imports `env.ts` therefore ran against the developer's local configuration. A realistic local `.env` (`OFFRAMP=testanchor`) makes `KYC_ENCRYPTION_KEY` required and the import throws; CI has no `.env` at all, so `OFFRAMP` defaulted to `mock` and the requirement never triggered. Turbo's cache compounded it by replaying stale green results | `44ef376` | `apps/api/vitest.config.ts` — `test.env` now pins `STELLAR_NETWORK`, `OFFRAMP` and `KYC_ENCRYPTION_KEY`, so a local run reproduces CI rather than the developer's machine. Verified with `npx turbo run test --force` (cache bypassed): 433 passing, 0 failing |
| 2026-08-06 | BUG-6.7 | An API key holding only the default scope set (`links:read`, `links:write`, `webhooks:manage`) could initiate a cash-out — the endpoint that moves money. Verified against a running instance: the request reached the business logic and was refused with `409 Link must be paid to cash out`, i.e. by link state rather than by authorization, so on a paid link it would have succeeded | `services/api-keys.ts` documents that `offramp:initiate` is deliberately excluded from `DEFAULT_SCOPES` because it moves money, and `middleware/auth.ts` shows the intended `app.post("/:id/cash-out", requireScope("offramp:initiate"), …)` usage — but `routes/links.ts` never mounted the guard. The scope existed, was documented, and was enforced nowhere. `GET /:id/cash-out/quote` and `GET /:id/offramp-requirements` were likewise ungated, the latter returning payout field descriptors to a default-scope key | `bb7a626` | `apps/api/test/api-key-auth.test.ts` · *"a default-scope key cannot initiate a cash-out"*, *"a key that opted into offramp:initiate reaches the handler"*, *"the off-ramp read routes require links:read"* |
| 2026-08-06 | BUG-6.8 | The strict rate limiter could be bypassed completely by any unauthenticated caller. Measured with `RATE_LIMIT_STRICT_MAX=20`: 40 requests to `POST /links` carrying a randomly-varied `Authorization: Bearer ak_live_…` header all passed the limiter (40 × 401), while the same 40 requests with no header were correctly throttled (5 × 401, 35 × 429). This defeated the limiter on exactly the routes it protects — link creation, cash-out and key management | `apiKeyRateLimitKey` derived the bucket from `bearer.slice(0, KEY_PREFIX_LEN)` after only `looksLikeApiKey`, a string test on caller-supplied input performed before any verification. Rotating the characters after `ak_live_` therefore minted a fresh, empty bucket on every request | `9fe650f` | `apps/api/test/api-key-auth.test.ts` · *"falls back to the IP bucket for a forged prefix"*, *"buckets a real key by its own prefix"*, *"stops bucketing by prefix once the key is revoked"*. Re-measured live: 20 pass / 20 × 429, matching the configured maximum |
| 2026-08-06 | BUG-4.12 | Any authenticated seller could wipe every seller's demo links. Verified: a second seller, registered seconds earlier via SEP-10, called `POST /demo/reset` and got `200` | `requireSeller` proves only that the caller is *a* seller, and SEP-10 registration is open (`createIfAbsent`), so any keypair holder can obtain a session. `routes/demo.ts` then called `links.deleteDemo()` with no seller argument, deleting every `is_demo` row in the table — including the seeded links the README directs visitors to | `255a82c` | `apps/api/test/demo-route.test.ts` · *"scopes the delete to the calling seller, not every seller"* |
| 2026-08-06 | BUG-4.13 | A payment link titled `=cmd|'/c calc'!A1` was written verbatim into `GET /links/export/csv` and is evaluated as a formula when the export is opened in Excel, LibreOffice or Google Sheets. Reproduced end to end against a running instance | `routes/links.ts` doubled embedded quotes per RFC 4180 but never guarded the leading characters that make a spreadsheet treat a cell as a formula (`=`, `+`, `-`, `@`, tab, CR). Reconciliation exports are routinely forwarded to accountants and finance tooling, so the blast radius is not limited to the seller who set the title | `cc14a7f` | `apps/api/test/csv-export.test.ts` · *"prefixes the four formula-trigger characters with an apostrophe"* and *"guards leading tab and carriage return, which Excel also treats as formula starts"* |
| 2026-08-06 | BUG-4.15 | `GET /links/:id` is unauthenticated by design, but returned the entire stored row — so anyone holding a link id could read the seller's realized FX rate, the indicative-vs-firm spread, the anchor fees they paid, and the internal `sellerId` | The handler returned `result` straight from `service.getLink()`. The comment above it claimed it "returns only what the checkout page renders", but nothing enforced that, so every column added to `PaymentLink` since — the whole `offramp*` block — was silently published | `cc14a7f` | `apps/api/test/routes/links.test.ts` · *"does not leak seller identity or off-ramp economics to an unauthenticated caller"*, which asserts both the absence of `sellerId`/`offramp*` and the presence of every field the checkout surface consumes |
| 2026-08-06 | BUG-8.8 | `GET /metrics` compared the bearer token with `!==`, leaking a prefix-match oracle through response timing, and accepted the token as a `?token=` query parameter where proxies, browser history and Referer headers capture it | Plain string comparison short-circuits on the first differing byte. The query-parameter path predates the header form and was never withdrawn | `2d1306d` | `apps/api/test/metrics-route.test.ts` — existing coverage for the accept/reject paths still passes against the constant-time comparison; `?token=` is retained deliberately (documented interface, existing test) and now logs `metrics.deprecated_query_token` |
| 2026-08-06 | BUG-4.14 | An `Idempotency-Key` reused across two different endpoints either replayed the other endpoint's stored response or returned a spurious `409 idempotency_key_reuse`. The first case can hand back a success-shaped response for an operation that never executed — on the cash-out endpoint | `middleware/idempotency.ts` looked rows up by `(key, sellerId)` while writing an `endpoint` column it never compared | `0e84e96` | `apps/api/test/routes/idempotency.test.ts` — existing replay/conflict coverage now runs against endpoint-namespaced keys. The stored key is namespaced rather than the primary key widened, because the table PK is `(key, seller_id)` and changing it needs the real migration tracked by issue 4.1 |
| 2026-08-06 | BUG-8.10 | `turbo run test` intermittently failed two `packages/core` fast-check property tests with `Error: Test timed out in 5000ms` — a random red build with no counterexample and no code change behind it | `packages/core/vitest.config.ts` set no `testTimeout`, so it used vitest's 5s default. The property suites take ~1.2–1.7s in isolation but exceed 5s when all six package suites run concurrently under turbo. `apps/api/vitest.config.ts` already carried this exact fix and its rationale; core never got it | `01c5d72` | `packages/core/vitest.config.ts` — `testTimeout`/`hookTimeout` raised to 10s, matching apps/api. Verified by reproducing the failure deterministically with `turbo run test --force`, then three consecutive clean loaded runs |
| 2026-08-06 | BUG-6.6 | `GET /seller/kyc` and `PUT /seller/kyc` both returned `200` to a caller sending no credentials of any kind — verified against a running instance. On the deployed configuration (`OFFRAMP=testanchor`) the GET served the seller's decrypted SEP-12 identity to anyone on the internet, and the PUT let anyone overwrite it and submit it to the live anchor | `routes/kyc.ts` was mounted in `index.ts` with no auth middleware, and resolved the seller with `sellers.getDefault()` rather than from a token. Every other seller route had been moved behind `buildAuthMiddleware` when wallet-native auth landed; this one was missed, so the AES-256-GCM at-rest encryption in `crypto/pii.ts` was protecting data that an unauthenticated GET then handed over in plaintext | `5a01d02` | `apps/api/test/kyc-route-auth.test.ts` · *"refuses an unauthenticated read of the seller's identity"* (also asserts the PII never appears in the response body), *"refuses an unauthenticated write of the seller's identity"*, *"refuses an authenticated key that lacks offramp:initiate"*, *"serves the authenticated seller, resolved from the token rather than getDefault()"* |
| 2026-08-06 | BUG-8.11 | `turbo run test` intermittently failed one of two `anchor-health` circuit-breaker tests with `AssertionError: expected 0 to be greater than 0`, or a false `isAvailable()` — a different test each time, with no code change behind it | `snapshot()` and `isAvailable()` both call `tickState()`, which auto-promotes `open → half_open` once `cooldownMs` has elapsed. The two tests asserted the breaker was *still open* while configured with 5ms and 50ms cooldowns — windows shorter than ordinary scheduler jitter — so under concurrent suite execution the promotion won the race. One test expressed its assertion as a `Date.now()` conditional, which turned the failure into an opaque `0 > 0` | `dbaa194` | `apps/api/test/anchor-health.test.ts` — both cooldowns raised to a shared 400ms constant and the `Date.now()` conditional replaced with a direct state assertion plus an explicit `half_open` step. Verified by reproducing under CPU contention, then three consecutive clean loaded runs |

---

## Adding a row (required for every bug-fix PR)

1. Open `docs/FIXLOG.md`.
2. Append a row to the table with all five columns filled.
3. The **Regression test** column must name a real test that would have caught the bug.
   If no test exists yet, the test must be written as part of the same PR — a row
   that says "none" is not acceptable.

See `.github/PULL_REQUEST_TEMPLATE.md` for the PR checklist item that enforces this.
